#include<stdio.h>
#include<stdlib.h>

int main(){
    int idade, olhoA, olhoV, olhoC, olhoP, cabeL, cabeP, cabeC, cabeR, quantM, quantF;
    float salario;
    char sexo[1], olho[1], cabe[1];

    do
    {
        printf("Digite seu sexo(m/f):");
        fgets(sexo, 1, stdin);

    } while (strsexo != 'm' || sexo != 'f');
    
    printf("%s", sexo);
    return 0;
}